CREATE DATABASE memo_db;

CREATE TABLE Memo_table
(id         serial,
 title      VARCHAR(50)  ,
 memo       VARCHAR(500) ,
 PRIMARY KEY (id));

CREATE VIEW Memo_view (id, title, memo)
AS
SELECT *
  FROM Memo_table;
  